{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm --filter @clinic/shared build && pnpm --filter @clinic/api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @clinic/api run start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
