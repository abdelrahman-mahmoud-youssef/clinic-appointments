{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm --filter @clinic/shared build && pnpm --filter @clinic/web build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @clinic/web run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
